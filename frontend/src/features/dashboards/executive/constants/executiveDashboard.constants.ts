import type { Branch } from "../types/executiveDashboard.types";

export const ALL_BRANCHES_VALUE = "all";

export const branches: Branch[] = [
  {
    id: "eldoret",
    name: "Eldoret Branch",
  },
];

export const executiveScopeNote =
  "Executive view is read-only for operational records. Only exception and high-value approvals are actionable here.";
