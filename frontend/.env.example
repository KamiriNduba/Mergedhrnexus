# Django API URL without a trailing slash
# In development this may be omitted: Vite proxies /api to http://127.0.0.1:8000.
# Set this to the deployed backend URL in production.
VITE_API_BASE_URL=/api
